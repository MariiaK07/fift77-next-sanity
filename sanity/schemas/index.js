import hero from './hero';
import classes from './classes';
import schedule from './schedule';
import classItem from './classItem';

const schemas = [hero, classes, schedule, classItem];

export default schemas;
